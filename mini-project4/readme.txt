1. Copy rotors from where you have it install into src

2. In terminal run:
	./setup.sh

3. Then source;
	source devel/setup.bash

4. To spawn a drone in the world, run.
	roslaunch rotors_gazebo rovi.launch 
